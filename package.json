{
  "name": "ancient-order-backend",
  "version": "0.1.0",
  "description": "Ancient Order Act 1 prototype backend - personality-driven RPG",
  "type": "module",
  "main": "dist/api/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/api/index.js",
    "dev": "tsx watch src/api/index.ts",
    "dev:all": "concurrently \"npm run dev\" \"npm run dev --prefix client\"",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "format": "prettier --write src/",
    "verify": "npm run typecheck && npm run lint && npm test && npm run verify --prefix client"
  },
  "keywords": ["rpg", "game", "backend", "typescript"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fastify": "^5.3.2",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "concurrently": "^9.0.0",
    "@types/uuid": "^9.0.7",
    "@typescript-eslint/eslint-plugin": "^8.26.0",
    "@typescript-eslint/parser": "^8.26.0",
    "eslint": "^9.21.0",
    "prettier": "^3.5.2",
    "tsx": "^4.19.3",
    "typescript": "^5.3.0",
    "vitest": "^3.0.8"
  }
}
